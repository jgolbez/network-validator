<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Task Verification Report</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            background-color: #f5f5f5;
            color: #333;
            line-height: 1.6;
            padding: 20px;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            text-align: center;
        }
        .header h1 {
            margin-bottom: 10px;
            font-size: 28px;
        }
        .header p {
            opacity: 0.9;
            font-size: 14px;
        }
        .timestamp {
            font-size: 12px;
            opacity: 0.8;
            margin-top: 15px;
        }
        .content {
            padding: 30px;
        }
        .checks-container {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }
        .check {
            border: 2px solid #ddd;
            border-radius: 6px;
            padding: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: all 0.3s ease;
        }
        .check.pass {
            border-color: #4caf50;
            background-color: #f1f8f4;
        }
        .check.fail {
            border-color: #f44336;
            background-color: #fef3f2;
        }
        .check.error {
            border-color: #ff9800;
            background-color: #fff8f1;
        }
        .check-info {
            flex: 1;
        }
        .check-name {
            font-size: 16px;
            font-weight: bold;
            margin-bottom: 5px;
            color: #333;
        }
        .check-description {
            font-size: 13px;
            color: #666;
        }
        .check-status {
            font-size: 18px;
            font-weight: bold;
            padding: 8px 16px;
            border-radius: 4px;
            min-width: 80px;
            text-align: center;
        }
        .check-status.pass {
            color: #4caf50;
            background-color: rgba(76, 175, 80, 0.1);
        }
        .check-status.fail {
            color: #f44336;
            background-color: rgba(244, 67, 54, 0.1);
        }
        .check-status.error {
            color: #ff9800;
            background-color: rgba(255, 152, 0, 0.1);
        }
        .footer {
            background-color: #f9f9f9;
            padding: 20px 30px;
            border-top: 1px solid #ddd;
            font-size: 12px;
            color: #666;
            text-align: center;
        }
        .summary-section {
            background-color: #f9f9f9;
            padding: 20px 30px;
            border-bottom: 1px solid #ddd;
            display: flex;
            justify-content: space-around;
            gap: 20px;
        }
        .summary-item {
            text-align: center;
        }
        .summary-value {
            font-size: 24px;
            font-weight: bold;
            color: #667eea;
        }
        .summary-label {
            font-size: 12px;
            color: #666;
            margin-top: 5px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Task Verification</h1>
            <p>Checking your network configuration against task requirements</p>
            <div class="timestamp">Generated: {{ run_timestamp }}</div>
        </div>

        <div class="summary-section">
            <div class="summary-item">
                <div class="summary-value" style="color: #4caf50;">{{ pass_count }}</div>
                <div class="summary-label">Passed</div>
            </div>
            <div class="summary-item">
                <div class="summary-value" style="color: #f44336;">{{ fail_count }}</div>
                <div class="summary-label">Failed</div>
            </div>
            <div class="summary-item">
                <div class="summary-value" style="color: #ff9800;">{{ error_count }}</div>
                <div class="summary-label">Errors</div>
            </div>
        </div>

        <div class="content">
            <div class="checks-container">
                {% for constraint in constraints %}
                <div class="check {{ constraint.status|lower }}">
                    <div class="check-info">
                        <div class="check-name">{{ constraint.name }}</div>
                        <div class="check-description">{{ constraint.description }}</div>
                    </div>
                    <div class="check-status {{ constraint.status|lower }}">
                        {{ constraint.status }}
                    </div>
                </div>
                {% endfor %}
            </div>
        </div>

        <div class="footer">
            <p>This report verifies that your configuration meets all task requirements.</p>
        </div>
    </div>
</body>
</html>
